# robot-elevator-saga

```js
const elevatorSaga = require('robot-elevator-saga')

elevatorSaga.run(
  {
     init: ...,
     updater: ...
  },
  0, // Challenge #0
  (passed, metrics) => {
     // if passed, passed = true
  }
);
```
